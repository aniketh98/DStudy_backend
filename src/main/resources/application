server:
  port: 5432
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://3.143.252.3/backenddb
    username: rootuser
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
